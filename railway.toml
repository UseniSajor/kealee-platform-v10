[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh /app/scripts/railway-next-start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
