[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd /app/services/api && node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
