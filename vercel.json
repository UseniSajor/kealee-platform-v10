{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "next build",
  "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" == claude/* ]]; then exit 0; fi && exit 1"
}
