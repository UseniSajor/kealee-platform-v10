{
  "buildCommand": "pnpm build --filter=m-project-owner",
  "outputDirectory": "apps/m-project-owner/.next",
  "installCommand": "corepack enable && corepack prepare pnpm@8.12.0 --activate && pnpm install",
  "framework": "nextjs"
}
