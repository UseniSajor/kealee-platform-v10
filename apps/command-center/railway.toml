[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.command-center"
watchPatterns = ["apps/command-center/**", "packages/**"]

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
