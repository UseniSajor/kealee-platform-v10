[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/command-center/Dockerfile"
watchPatterns = ["apps/command-center/**", "packages/**"]

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
