[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.admin-console"
watchPatterns = ["apps/admin-console/**", "packages/**"]

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
