{
  "buildCommand": "pnpm build --filter=os-admin",
  "outputDirectory": "apps/os-admin/.next",
  "installCommand": "corepack enable && corepack prepare pnpm@8.12.0 --activate && pnpm install",
  "framework": "nextjs"
}
