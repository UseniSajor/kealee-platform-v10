[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.portal-owner"
watchPatterns = ["apps/portal-owner/**", "packages/**"]

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
