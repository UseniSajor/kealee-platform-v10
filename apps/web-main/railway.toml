[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.web-main"
watchPatterns = ["apps/web-main/**", "packages/**"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
