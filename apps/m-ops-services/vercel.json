{
  "buildCommand": "pnpm build --filter=m-ops-services",
  "outputDirectory": "apps/m-ops-services/.next",
  "installCommand": "corepack enable && corepack prepare pnpm@8.12.0 --activate && pnpm install",
  "framework": "nextjs"
}
