{
  "name": "@kealee/knowledge",
  "version": "0.1.0",
  "private": true,
  "description": "Kealee Construction Intelligence — knowledge artifact registry, provenance, generation records and learning events (Phase 1)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./src/index.ts", "require": "./dist/index.js", "default": "./src/index.ts" }
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kealee/database": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^1.6.0"
  }
}
