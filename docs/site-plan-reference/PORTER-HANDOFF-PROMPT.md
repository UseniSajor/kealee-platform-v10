# Handoff prompt — Porter Subdivision Lots 1 & 2 permit set

Paste everything below the line into a new agent session.

---

You are continuing an in-progress task in the Kealee Platform monorepo at
`/mnt/c/Users/Tim Chamberlain/Documents/kealee-platform-v10`.

## The task

Produce a **submission-ready Prince George's County, MD site plan** for
**Porter Subdivision Lots 1 & 2 — 1005 and 1009 Rollins Ave, Capitol Heights, MD**,
generated by `packages/spatial-engine`. Both lots appear on one condensed sheet.

Generate with:

```bash
cd "packages/spatial-engine"
pnpm tsx scripts/generate-subdivision.ts
```

Canonical output: `output/site-plans/porter-subdivision-permit-set.pdf`

## Standing constraints — these are absolute

1. **Do NOT commit and do NOT push.** The user has said this twice and it is a
   standing order. This work is internal.
2. **Never put internal reasoning on a plan.** No "reason not computed", no
   "assumed", no software notes, no derivation commentary. If you lack data,
   go find it — do not annotate the absence.
3. **Never name PGAtlas (or any GIS source) anywhere on the sheet.**
4. **The recorded plat supersedes every other source.** Boundary, bearings,
   distances and areas come from the plat. PGAtlas supplies only the added
   layers: contours, zoning, street centrelines, adjacent parcels, soils.
5. **Never draw anything that is not on the Rollins easement document.** Four
   separate derivations of the easement corridor have been wrong. Transcribe;
   do not infer.
6. **Render and inspect every run.** Never report a PDF finished from a status
   line — a five-sheet set once silently rendered 65 pages. Rasterise the PDF
   and actually look at it before telling the user it is done.

## Source documents — you have all three

In `existing site plans/`:
- the **Rollins Ave recorded plat** (Porter Subdivision) — boundary of record
- **two Rollins easement PDFs** (WSSC water/sewer) — **page 2 is the design to copy**
- `Rollins Ave lots.pdf` — approved PG plan, drafting convention reference
- the **Kingsworth basement floorplan** — the building footprint source
- the **Yocum** approved set — sidewalk, curb & gutter, apron style and colour,
  data-block layout, and the county standard details

Read `docs/site-plan-reference/APPROVED-PLAN-ANALYSIS.md` before changing sheet
layout, and `docs/site-plan-reference/CHECKLIST-FINDINGS.md` before copying any
note off the Yocum sheet (one of its citations, COMAR 26.17.1.08 G, is wrong and
must not be reproduced).

## Open defects — in priority order

### 1. The 20 ft public utility easement is still in the wrong place
The user's own description: *"the public 20' utility is between lot 12 & lot 13
at the place where lot 1 adjoins"*, and *"the easement should only be on lot 13,
not touching any other lot"*, *"place the easement in the exact place the WSSC
easement PDF illustrates"*.

Everything derived so far has failed. **Transcribe it from page 2 of the
easement PDF.** An affine georeference was attempted and rejected: Lot 2's
frontage transcribed as 157.49 ft against the plat's 134.64 ft, 17% off — it was
discarded rather than drawn from, which was correct. Do not resurrect it without
an independent check that passes.

Field note: PGAtlas parcel layer field is `PROP_ID`, not `PROPID`. Near the
Porter west corner (E 1340254.71, N 440194.16, MD state plane ft):

| PROP_ID | area | distance from corner | note |
|---|---|---|---|
| 34811 | 9,098 sf | 0.0 ft, NW | Lot 13 candidate (has cul-de-sac arc, 95 vertices) |
| 348854 | 9,596 sf | 0.0 ft | Porter Lot 1 itself |
| 34805 | 7,762 sf | 31.6 ft, S | Lot 12 candidate |
| 34810 | 6,872 sf | 44.1 ft | |

Their shared boundary measures 33.1 ft at bearing 77.32°, from
E1340253.47 N440162.58 to E1340285.79 N440169.85 — **that does not read like a
corridor**, so the parcel-geometry approach looks like another dead end. Prefer
the document.

### 2. Lot 2's sewer and water lines are not inside the easement area
Reported by the user and **not yet fixed**. Lot 2's lines must run to the rear of
Lots 1 and 2 to connect, then reach the main through the easement — not across
open lot. Lot 1's lines go straight to the Lot 12 easement.

### 3. The aprons do not meet the curb and gutter
Requested and not yet delivered. The apron must touch the property line and the
curb so a car is never crossing a grass strip to reach it.

### 4. Lot 2's stormwater management still fouls the walk
It must clear the sidewalk and every structure. This has been reported at least
three times.

## Drawing conventions already established — keep them

- Water `#1565c0`, dash `[12,4]`, ticked `W`. Sanitary `#2e7d32`, dash
  `[14,3,2,3]`, ticked `S`. Storm `#6d4c41`. They must stay visually distinct.
- Water and sewer run **adjacent, in the same trench** — not spaced apart.
- **No boxes** anywhere around the easement or the utility runs. Label the
  easement **on the line**: UTILITY EASEMENT / PRIVATE UTILITY EASEMENT.
- No dotted box inside the building footprints. Address text sits fully inside
  the footprint.
- The walk, planting strip and curb belong in the **20 ft public right-of-way
  dedication**, never in the 10 ft PUE.
- The apron is distinguished from the driveway by colour/texture, Yocum style.
- Street trees sit in the planting strip at the front property line.
- The full street is drawn — both sides, with the lots across Rollins.
- Building footprint 46 ft wide × 26 ft deep, 10 ft garage; 30 ft front setback,
  8 ft side. Lot 2's north-west line is treated as a **side** yard.

## Key files

| Path | What |
|---|---|
| `packages/spatial-engine/scripts/generate-subdivision.ts` | the generator entry point |
| `packages/spatial-engine/src/site-plan/site-improvements.ts` | driveway, walk, stoop, apron, sidewalk, curb |
| `packages/spatial-engine/src/site-plan/design.ts` | `generateDesign()` — utility runs, trees, SWM placement |
| `packages/spatial-engine/src/self-perform/lot-package.ts` | `buildLotPackage()`, clipping, tap points |
| `packages/spatial-engine/src/sheets/render-pdf.ts` | pens, details band, county standard details |
| `packages/spatial-engine/src/site-plan/drainage.ts` | rational-method drainage computations |
| `packages/spatial-engine/src/jurisdictions/noaa-atlas14.ts` | NOAA Atlas 14 point estimates |
| `output/site-plans/porter-lot1.plat.json`, `porter-lot2.plat.json` | per-lot programme |
| `output/site-plans/porter-subdivision.plat-record.json` | adjoiners, platted easements, monuments |

## Traps that have already cost hours

- **Plat rings wind clockwise; the envelope normalises to CCW.** Any code
  reading a raw parcel ring must call `normaliseRing` first, or improvements
  land 100% outside the lot.
- **`contained()` drops features silently.** It has discarded a sewer run, the
  street trees, an SWM facility and service laterals — each found only by eye.
  It should clip, not drop. Check what reached the paper.
- **The canonical PDF gets file-locked by FoxitPDFEditor** (PID 28644 is holding
  it right now; the last successful canonical write was 11:24 and every run
  since has fallen back to a timestamped file). The generator prints a loud
  `!! IS LOCKED` warning — do not let it scroll past, and never redirect the run
  to `/dev/null`. Ask the user to close Foxit, and verify the file's mtime
  actually moved before reporting an update.
- **`princegeorgescountymd.gov` blocks Anthropic egress IPs** — `WebFetch` gets
  403 where a local `curl` gets 200. Try locally before concluding anything is
  unreachable.
- **PGAtlas: pass the street address alone**, no city/state/ZIP, minimum locator
  score 90.

## What is already done and should not be re-litigated

Setback compliance at the full 46×26 footprint; the single-sheet condensed
layout; DPW&T standard details 300.01 and 300.07 embedded from the Yocum set;
tree canopy at 20% of net tract (RSF-65, Sec. 25-128 Table 1); SWM required on
both lots under Sec. 32-174(a)(3), neither in the Critical Area; sidewalk on one
side per Sec. 23-135; drainage computations from real NOAA Atlas 14 Vol. 2
Ver. 3 data for 38.8752/-76.9019; disturbance quantified at 6,856 SF.

## Start here

Open page 2 of the Rollins easement PDF, read the water and sewer layout off it
directly, and place the 20 ft easement and both lots' service runs to match it.
Then fix the aprons and Lot 2's SWM, regenerate, rasterise the PDF, inspect
every sheet, and report what you actually see. No commits, no pushes.
